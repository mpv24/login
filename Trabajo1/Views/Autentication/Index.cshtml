@model Trabajo1.Models.Usuario

@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}

<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: #b39ddb; /* Fondo morado claro */
    }

    .login-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background: #b39ddb; /* Fondo morado claro */
    }

    .login-box {
        background: #e1bee7; /* Fondo morado claro más suave */
        padding: 2rem;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 400px;
        text-align: center;
    }

    .login-icon {
        margin-bottom: 1.5rem;
    }

        .login-icon img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #7e57c2; /* Borde morado oscuro */
        }

    .login-box h2 {
        margin-bottom: 1.5rem;
        font-size: 2rem;
        color: #7e57c2; /* Morado oscuro */
    }

    .input-group {
        margin-bottom: 1rem;
        text-align: left;
    }

        .input-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: #6a1b9a; /* Morado más oscuro */
            font-weight: 500;
        }

        .input-group input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #d1c4e9; /* Morado claro */
            border-radius: 4px;
            font-size: 1rem;
            box-sizing: border-box;
        }

            .input-group input:focus {
                border-color: #6a1b9a; /* Morado más oscuro */
                outline: none;
                box-shadow: 0 0 0 0.2rem rgba(106, 27, 154, 0.25);
            }

    .btn-primary {
        background-color: #6a1b9a; /* Morado más oscuro */
        border: none;
        padding: 0.75rem 1.5rem;
        color: #ffffff;
        border-radius: 4px;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.3s ease;
        width: 100%;
    }

        .btn-primary:hover {
            background-color: #4a148c; /* Morado más oscuro */
        }

    .actions {
        margin-top: 1.5rem;
    }

        .actions a {
            color: #6a1b9a; /* Morado más oscuro */
            text-decoration: none;
            font-weight: 500;
        }

            .actions a:hover {
                text-decoration: underline;
            }
</style>

<div class="login-container">
    <div class="login-box">
        <div class="login-icon">
            <img src="~/img/icono.jpeg" alt="User Icon" />
        </div>
        <h2>InnovateX</h2>
        <form asp-action="Index" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="input-group">
                <label for="Usuario1"><i class="fas fa-user"></i> Usuario</label>
                <input asp-for="Usuario1" class="form-control" required />
            </div>
            <div class="input-group">
                <label for="Pass"><i class="fas fa-lock"></i> Contraseña</label>
                <input asp-for="Pass" class="form-control" type="password" required />
            </div>
            <button type="submit" class="btn btn-primary">Iniciar Sesión</button>
        </form><br />
        <div class="actions">
            ¿No tienes una cuenta? <a href="@Url.Action("Crear", "Autentication")"> Regístrate</a>
        </div>
    </div>
</div>